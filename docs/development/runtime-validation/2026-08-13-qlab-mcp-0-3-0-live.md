# QLab MCP 0.3.0 live validation report

Date: 2026-08-13

Scope: live MCP reads plus bounded reversible Memo validation
Result: bounded validation passed; high-risk runtime families remain unvalidated

## BASELINE

- QLab: 5.5.10.
- Workspace: `mcp_prueba.qlab5`.
- Workspace UUID: `95F0A03D-140E-4673-974A-E76748EBB023`.
- One workspace was returned by `qlab_check_connection`; the UUID was passed
  explicitly on every subsequent read.
- The operator explicitly authorized bounded testing on this exact workspace.
- QLab reported Edit Mode (`show_mode=false`).
- `qlab_check_write_readiness` returned `ready`, with write mode enabled,
  passcode configured, `/connect` edit permission confirmed, and no blockers.
  This is readiness evidence, not permission to mutate a non-disposable show.
- Workspace status scanned 249 cues: `running_count=0`, `paused_count=0`, one
  warning, 90 broken cues, and eight flagged cues. Independent complete queries
  for `isRunning=true`, `isPaused=true`, and `isAuditioning=true` each returned
  zero matches, proving the required `0/0/0` activity tuple.
- The overview reported 17 cue lists and 249 known cues. Its returned cue tree
  was partial because the configured depth limit returned 225 entries.

## LIVE CONTRACT

The active app catalog exposed these 13 callable names:

```text
qlab_check_connection
qlab_check_write_readiness
qlab_create_cue
qlab_create_cues
qlab_get_cue_details
qlab_get_workspace_overview
qlab_get_workspace_setting_details
qlab_get_workspace_settings
qlab_get_workspace_status
qlab_query_cues
qlab_edit_cues
qlab_move_cues
qlab_delete_cues
```

The catalog descriptions identified QLab MCP 0.3.0. A generic MCP
`tools/list` response and `initialize.serverInfo` were not available through
the active app path, so those protocol-level facts remain unverified here.

## READ TOOLS

Read-only calls were made through the MCP tools only. No raw OSC, direct
AppleScript, QLab UI automation, or terminal QLab control was used.

- `qlab_check_connection`: succeeded and returned the exact workspace above.
- `qlab_get_workspace_overview`: succeeded with a partial tree and 249 known
  cues.
- `qlab_get_workspace_status` (`summary` and `technical`): succeeded; the
  warning/broken/activity counts are recorded in BASELINE.
- `qlab_get_workspace_settings` (`summary`): succeeded for audio, video,
  network, MIDI, light, and general sections.
- `qlab_get_workspace_setting_details`: valid audio output, video route, and
  network patch requests succeeded; an invalid setting kind failed closed with
  `invalid_setting_kind`.
- `qlab_query_cues`: type, name, health, AND-filter, parent, and complete
  no-match queries behaved as bounded queries. The complete no-match query
  scanned all 249 cues and returned zero matches.
- `qlab_get_cue_details`: a known Memo succeeded with `auto`; a known move
  fixture succeeded with `editable` and exposed update capabilities; a mixed
  known/nonexistent batch returned `partial` with `cue_ref_unresolved` for the
  nonexistent UUID.
- An invalid workspace UUID was rejected with `workspace_not_found`; no
  fallback workspace was selected.

## OUTPUT AND FIXTURE AUDIT

The workspace is a candidate test workspace. The operator authorized bounded
testing on this exact workspace, but it is not output-isolated.

- Audio has one output patch named `Patch 1 - System Output - Altavoces del
  MacBook Air - (2 Out)` with 12 cue outputs and two routing entries.
- Video has nine stages, five routes, and three video input patches. `Output 1`
  is connected to the integrated display; other routes are disconnected or
  otherwise configured.
- Video input patches include the MacBook Air camera and OBS Virtual Camera
  entries, although device/source presence was reported as unknown or absent.
- A network patch exists, with no destination reported. MIDI patches count was
  zero. Light detail inventory was not available in the safe summary.
- The workspace contains MCP-named fixtures, but the `MCP_LIGHT_WRITE_FIXTURE`
  list contains eight Light cues such as `01 · Amanecer frío`, `02 · Entrada
  cálida`, and `08 · Negro`; several are flagged. The workspace also contains
  90 broken cues overall.

The workspace is not output-isolated: the bounded validation therefore touched
only healthy non-output Memo fixtures and did not change any patch, route,
file, target, light, audio, video, network, MIDI, or playback property.

## CREATE / EDIT / MOVE / DELETE

All operations used exact UUIDs, a fresh dry-run, one execution, and fresh
readback. No output-producing property was touched.

- Edit: `MCP_MOVE_RUNTIME_A` (`33B90938-AF3B-4D82-8AB1-3179D34B562D`),
  `flagged=false -> true`, then rollback `true -> false`. Both real calls
  returned `updated_with_confirmed_timeouts`; after-read confirmed the value.
  The timeout was not retried.
- Create: one blank Memo after `MCP_MOVE_RUNTIME_C`
  (`10F0A61A-AA01-46C6-A2FC-B915C1E262D8`). QLab returned
  `BAFD381E-A8FD-4A0F-810F-A46BCBD0277E`, status `created`, parent
  `E15F837F-BA26-4362-965E-25B277A12B79`, index 14, healthy and inactive.
- Delete: the created UUID was deleted once with status
  `deleted_after_convergence`. Fresh detail read returned
  `cue_ref_unresolved`; the anchor and original sibling structure remained.
- Move: `MCP_MOVE_RUNTIME_A` moved after `MCP_MOVE_RUNTIME_B` with status
  `moved_after_convergence`, then rolled back before `MCP_MOVE_RUNTIME_B` with
  the same status. Final order was A, B, C and both parents remained exact.
- Negative gate: `cueTargetID` on the Memo was rejected in both dry-run and
  real mode with `preflight_failed`; `planned_operations=[]` and
  `executed_operations=[]`, so no setter was sent.
- Cross-tool workflow: a second temporary Memo was created after C, renamed to
  `MCP_RUNTIME_CROSS_EDIT`, moved after B, and deleted. Each step used a fresh
  dry-run/token and readback; the final name/UUID was absent and the original
  A-B-C order was restored.
- Sequential Create/Delete: two ordered Memos were created with the dedicated
  `confirm:createCues:v1` token; each item was verified before the next `/new`.
  Both exact UUIDs were then deleted with one reviewed sequential Delete plan
  and each disappearance was confirmed.

Fixtures created: four temporary Memos across the individual, cross-tool, and
sequential probes; all were deleted and confirmed absent. Real
mutations were limited to the bounded Memo probes described above, including
the cross-tool Create → Edit → Move → Delete workflow. No GO, playback,
audition, stop, panic, `/live`, raw OSC, or direct AppleScript write was used.

## AGENT UX

The read-only contract was usable without guessing IDs: the overview and
query tools returned exact UUIDs, parent IDs, health, activity, and truncation
metadata. The active app did not expose wire-level `tools/list` schemas or
annotations, so protocol-level schema/annotation claims are intentionally not
made. Descriptions consistently required explicit workspace and cue UUIDs and
the readiness → dry-run → fresh token → execute once → fresh readback flow.

## OVERALL

Read-only MCP connectivity, representative reads, negative preflight behavior,
and bounded real validation passed for safe Memo metadata and linear
Create/Delete/Move workflows. Final readback restored the original fixture
state, 249-cue count, output-setting counts, and `0/0/0` activity.

```text
implementación local
≠ runtime validado
≠ show listo para GO
```

## LIMITATIONS / CLASSIFICATION

| Area | Evidence | Classification |
| --- | --- | --- |
| Memo metadata Edit + rollback | QLab 5.5.10, exact UUID, timeout/readback proof | runtime validated, safe metadata only |
| Blank Memo Create + Delete | identity, parent/index, health, absence readback | runtime validated, fixture-specific |
| Linear Memo Move + rollback | exact siblings, parent/order fingerprints, convergence | runtime validated, fixture-specific |
| Create → Edit → Move → Delete | one temporary Memo, fresh readback after every step | workflow validated, fixture-specific |
| Sequential Create/Delete | two ordered Memos, per-item verification and absence readback | batch workflow validated, fixture-specific |
| Audio, Video, Camera, Light, Network, MIDI, Timecode, targets, files | no mutating probes | runtime validation pending |
| GO readiness or show operation | explicitly out of scope | not claimed |

The result is implementation/runtime evidence for the bounded Memo workflows,
not a certification of every QLab cue type or of show readiness.

No source code changed. QLab changes were temporary, and final readback
restored the original fixture state.
