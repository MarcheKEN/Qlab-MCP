# QLab MCP 0.3.0 live validation — read-only stop report

Date: 2026-08-13

Scope: live MCP reads only
Result: stopped before all writes

## BASELINE

- QLab: 5.5.10.
- Workspace: `mcp_prueba.qlab5`.
- Workspace UUID: `95F0A03D-140E-4673-974A-E76748EBB023`.
- One workspace was returned by `qlab_check_connection`; the UUID was passed
  explicitly on every subsequent read.
- QLab reported Edit Mode (`show_mode=false`).
- `qlab_check_write_readiness` returned `ready`, with write mode enabled,
  passcode configured, `/connect` edit permission confirmed, and no blockers.
  This is readiness evidence, not permission to mutate a non-disposable show.
- Workspace status scanned 249 cues: `running_count=0`, `paused_count=0`, one
  warning, 90 broken cues, and eight flagged cues. The status tool did not
  expose a separate auditioning counter, so the required `0/0/0` activity
  tuple was not fully proven.
- The overview reported 17 cue lists and 249 known cues. Its returned cue tree
  was partial because the configured depth limit returned 225 entries.

## LIVE CONTRACT

The active app catalog exposed these 13 callable names:

```text
qlab_check_connection
qlab_check_write_readiness
qlab_create_cue
qlab_create_cues
qlab_get_cue_details
qlab_get_workspace_overview
qlab_get_workspace_setting_details
qlab_get_workspace_settings
qlab_get_workspace_status
qlab_query_cues
qlab_edit_cues
qlab_move_cues
qlab_delete_cues
```

The catalog descriptions identified QLab MCP 0.3.0. A generic MCP
`tools/list` response and `initialize.serverInfo` were not available through
the active app path, so those protocol-level facts remain unverified here.

## READ TOOLS

Read-only calls were made through the MCP tools only. No raw OSC, direct
AppleScript, QLab UI automation, or terminal QLab control was used.

- `qlab_check_connection`: succeeded and returned the exact workspace above.
- `qlab_get_workspace_overview`: succeeded with a partial tree and 249 known
  cues.
- `qlab_get_workspace_status` (`summary` and `technical`): succeeded; the
  warning/broken/activity counts are recorded in BASELINE.
- `qlab_get_workspace_settings` (`summary`): succeeded for audio, video,
  network, MIDI, light, and general sections.
- `qlab_get_workspace_setting_details`: valid audio output, video route, and
  network patch requests succeeded; an invalid setting kind failed closed with
  `invalid_setting_kind`.
- `qlab_query_cues`: type, name, health, AND-filter, parent, and complete
  no-match queries behaved as bounded queries. The complete no-match query
  scanned all 249 cues and returned zero matches.
- `qlab_get_cue_details`: a known Memo succeeded with `auto`; a known move
  fixture succeeded with `editable` and exposed update capabilities; a mixed
  known/nonexistent batch returned `partial` with `cue_ref_unresolved` for the
  nonexistent UUID.
- An invalid workspace UUID was rejected with `workspace_not_found`; no
  fallback workspace was selected.

## OUTPUT AND FIXTURE AUDIT

The workspace is only a candidate test workspace, not a confirmed disposable
copy.

- Audio has one output patch named `Patch 1 - System Output - Altavoces del
  MacBook Air - (2 Out)` with 12 cue outputs and two routing entries.
- Video has nine stages, five routes, and three video input patches. `Output 1`
  is connected to the integrated display; other routes are disconnected or
  otherwise configured.
- Video input patches include the MacBook Air camera and OBS Virtual Camera
  entries, although device/source presence was reported as unknown or absent.
- A network patch exists, with no destination reported. MIDI patches count was
  zero. Light detail inventory was not available in the safe summary.
- The workspace contains MCP-named fixtures, but the `MCP_LIGHT_WRITE_FIXTURE`
  list contains eight Light cues such as `01 · Amanecer frío`, `02 · Entrada
  cálida`, and `08 · Negro`; several are flagged. The workspace also contains
  90 broken cues overall.

These facts do not prove operator ownership, disposability, or output
isolation. The workspace name and fixture names alone are insufficient.

## CREATE / EDIT / MOVE / DELETE

Dry-run-only probes were completed with exact UUIDs. Each returned a plan and
`executed_operations=[]`; the dedicated confirmation tokens were not retained
or reused.

- Edit: one healthy inactive Memo, `MCP_MOVE_RUNTIME_A`, planned `flagged=false
  -> true`; status `dry_run`.
- Create: one Memo after the exact `MCP_MOVE_RUNTIME_C` anchor; status
  `dry_run`, anchored placement and expected index recorded.
- Move: one exact-UUID move of `MCP_MOVE_RUNTIME_A` after
  `MCP_MOVE_RUNTIME_B`; status `planned`, parent and neighbor fingerprints
  recorded.
- Delete: one exact leaf plan for `MCP_MOVE_RUNTIME_C`; status `planned`,
  parent/neighbor/activity fingerprints recorded.

Real execution remains blocked. The campaign requires a clearly disposable
workspace with isolated or dummy outputs; that evidence is not present.
Therefore:

- fixtures created: none;
- real writes: none;
- rollbacks: none;
- cleanup actions: none;
- GO, playback, audition, stop, panic, `/live`, raw OSC, and direct
  AppleScript writes: none.

## AGENT UX

The read-only contract was usable without guessing IDs: the overview and
query tools returned exact UUIDs, parent IDs, health, activity, and truncation
metadata. The active app did not expose wire-level `tools/list` schemas or
annotations, so protocol-level schema/annotation claims are intentionally not
made. Descriptions consistently required explicit workspace and cue UUIDs and
the readiness → dry-run → fresh token → execute once → fresh readback flow.

## OVERALL

Read-only MCP connectivity, representative reads, and non-mutating dry-run
planning passed. The campaign did not reach real Create, Edit, Move, Delete,
rollback, or cleanup.

```text
implementación local
≠ runtime validado
≠ show listo para GO
```

## RELEASE BLOCKERS

1. Obtain explicit confirmation that this exact workspace is disposable and
   owned for destructive testing, or connect a disposable copy.
2. Establish output isolation/dummy-output evidence, including the audio,
   video, network, and light paths relevant to the selected fixture.
3. Reconfirm fresh `0/0/0` activity, exact inactive fixtures, parent/order
   fingerprints, and operation-specific dry-run tokens before any write.

No code or runtime behavior was changed by this validation.
